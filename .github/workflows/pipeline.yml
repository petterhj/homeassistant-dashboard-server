name: Pipeline

on:
  push:
    branches:
      - 'main'
    paths-ignore:
      - '.github/**'
      - 'dev/**'
      - 'inkplate/**'
      - '**/README.md'
  workflow_dispatch:

jobs:
  # tests:
  #   name: Run tests
  #   uses: ./.github/workflows/tests.yml

  push:
    name: Build and push
    # needs: [tests]
    uses: ./.github/workflows/push.yml
